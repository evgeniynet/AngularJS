import { Nav, ViewController } from 'ionic-angular';
export declare class PrivacyModal {
    private nav;
    private viewCtrl;
    constructor(nav: Nav, viewCtrl: ViewController);
    dismiss(item: any): void;
}
